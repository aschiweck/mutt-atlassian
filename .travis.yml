language: python
python:
  - "2.7"
# command to install dependencies
install: "pip install -e ."
# command to run tests
script: py.test
before_install:
  - pip install codecov
after_success:
  - codecov
